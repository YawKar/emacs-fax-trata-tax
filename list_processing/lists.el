(setq global-list '(rose violet daisy buttercup))

(message "%s" global-list)

(let ((nested-list '(1 2 (aurora benjamin)))) (message "%s" nested-list))

(let ((empty-list '())) (message "%s" empty-list)) ;; nil

